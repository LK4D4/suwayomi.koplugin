# Issues #59–#62 Palma evidence — 2026-09-26

Recorded issue controls: **demonstrated on physical Palma**. One initial scoped Open stalled in verification; subsequent uninstrumented controls passed after restart, but its cause remains unconfirmed. This record complements [desktop and automated evidence](issues-59-62-acceptance-2026-09-26.md), rather than establishing every possible runtime condition.

Runtime candidate: `aaa6d64865dc18e39ad9da831b18ffdffc4de738`; integration head at test start: `a333e485e1a72d2456f4b6ff6c09db9594f50bff` (documentation-only difference). No production code changed during this run. All **98** device runtime files matched the frozen source manifest before testing and again before restoration. The authenticated inspector was a temporary profile patch outside the payload.

## Environment and method

Physical Boox Palma, installed KOReader **v2026.03**, disposable Suwayomi **v2.3.2243**, Basic Auth. USB ADB reverse carried device requests to the owned server; ADB forward exposed the authenticated loopback inspector. Host loopback and WSL host-only relays connected those endpoints. This was not a Wi-Fi test.

At takeover KOReader was stopped and no ADB mappings or test inspector were present. The entire original profile was hashed and copied into a private host backup; all **1,220** backup files matched. The original device directory was renamed aside intact, and tests used a fresh profile plus synthetic Alpha/Beta archives. Server credentials, screenshots, logs, and raw settings stayed private.

Entry used native KOReader Search > Suwayomi. Actions used observed production widgets; native page-view controls established page 2 of a three-page archive. Independent server queries, persisted-state comparisons, PNG source-byte comparisons, archive/sidecar hashes, and screenshots supplemented UI observations. Screenshots of pending unread, saved policy, and deleted-category results were visually inspected. Fixture snapshots restored equivalent preconditions between scenarios.

## Results

| Issue | Physical controls and outcomes |
| --- | --- |
| **#59: bulk and mixed read action** | With one legacy archive and two eligible chapters, Download next 5 produced the two safe archives. All three archives' PNG pages matched source bytes. Legacy archive and ledger entry stayed unchanged. Mark previous as read reported an association refusal; independent server flags stayed unchanged. |
| **#59: auto policy and refill** | First 5 unread persisted and downloaded eligible chapters beside the legacy record; source bytes matched and legacy record/archive remained unchanged. First 10 unread survived a cold reader restart. Off persisted when selected from a settled chapter view. See the first-attempt observation below. Selection/filter/pathless permutations retain the separately recorded desktop and composed-spec coverage. |
| **#60: native return and progress** | Without diagnostic instrumentation, legacy Open resumed page 2/3. Native Go to Suwayomi returned all three server chapters, including undownloaded chapters; independent membership matched and ordinary Refresh succeeded. |
| **#60: offline and empty success** | With the owned server stopped, native return retained all three saved chapters and page-2 progress. Explicit Refresh reported connection failure and kept the list; Library navigation worked. A separate authenticated responder injected a successful empty chapter response while the real server was stopped. Native return showed no chapters; a cold offline restart retained that authoritative empty cache despite the preserved archive. |
| **#60: identity and obsolete response** | Conflicting source metadata with the same IDs kept native return local, with server actions withheld; Library still opened the full list. Suspending only the owned server process held a request during return. Navigating to Library before resuming the server prevented the obsolete response from reopening Chapters. |
| **#61: pending unread and context-dependent action** | Normal Mark as read created completed-sidecar/server-read evidence. With the reader stopped, an unknown-origin pending explicit unread was injected. Saved offline rendering, Refresh, and Library reopen showed Downloaded without Read. Ledger, return records, archive/sidecar hashes, and completed status stayed unchanged; no refill request appeared and server Read stayed true, proving no unknown-origin sync. A subsequent normal Open next unread selected that pending-unread chapter and resumed page 2. Preservation assertions preceded this explicit reading action. |
| **#61: foreign path** | A pathless pending choice with a foreign-owned guessed archive did not override Read or expose Downloaded, before or after Refresh. Saved records and file hashes stayed unchanged. |
| **#62: category selection and authority** | Deleting the selected category on the disposable server then pressing Refresh immediately showed All manga, matching independent membership. Back opened the updated picker without the deleted category. Renamed selection retained its ID and fresh name; existing empty category, Default, and All behaved correctly. Failed Refresh kept selected-category saved rows. Genuine empty server Library membership stayed empty despite local archives; files remained unchanged and fixture membership was restored afterward. |

## Observations and limits

The first normal scoped Open after a fresh download remained at **Verifying download** through bounded waits, with no visible worker process. A normal restart recovered it. An observation-only diagnostic patch then recorded successful verification completion; that patch was removed before all recorded issue acceptance controls. Final uninstrumented checks passed both existing scoped Open at page 2 and immediate Open after a fresh scoped download. The initial stall did not recur in those controls. Its cause is **unconfirmed**; this run does not claim a repair or establish whether it predates the candidate.

The first Off attempt immediately after cold-start entry left the stored policy at 10. Off succeeded after reopening its menu on a settled chapter view. That is consistent with existing protection against actions captured before background replacement, but this particular rejected/no-op attempt was not instrumented, so its cause is not proven.

Two setup problems were corrected before acceptance: Windows-generated CRLF in the private inspector token, and Windows-to-WSL loopback routing. Neither required production changes. Offline controls used owned-server stops or removal of the owned USB reverse route; the latter was restored in `finally`. Empty chapter success was an injected response. No physical Wi-Fi transition, sleep/wake, power-loss, real storage-exhaustion, or human e-ink usability assessment occurred. Additional identity and checked-write permutations retain the evidence layers recorded in the desktop report.

## Restoration and handoff

At **2026-09-26 13:36:12 UTC**, original profile restoration passed **1,220/1,220 SHA-256 comparisons**, zero mismatches. The original directory also matched before restoration, proving it stayed unchanged during tests. Original installed plugin/profile and KOReader v2026.03 were restored, app enabled and stopped as initially observed. No personal archive was opened or modified. ADB forward/reverse mappings returned to their original empty state.

Owned reader, server, response injector, and host/WSL relays stopped; listener checks confirmed no task ports remained. Eight task profile/fixture directories were retained together in a dedicated device evidence directory; private host backup, APK source inspection copy, manifests, screenshots, and helpers remain under the integration worktree's ignored Palma acceptance directory. All implementation worktrees remain retained. No push, merge, publication, or GitHub issue modification occurred.
